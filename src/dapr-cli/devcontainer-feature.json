{
  "name": "dapr-cli",
  "id": "dapr-cli",
  "version": "0.0.1",
  "description": "Install the Dapr CLI",
  "options": {
    "version": {
      "type": "string",
      "proposals": [
        "latest"
      ],
      "default": "latest",
      "description": "Enter a version (or latest)"
    }
  }
}